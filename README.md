Obtain all source files 
Create/obtain a Webex bot token; set WEBEX_ACCESS_TOKEN.
Add the bot to your target space; capture the room ID; set WEBEX_ROOM_ID.
Set approved users/domains/rooms in the WebexBot(...) call.
Configure proxy env vars if inside a restricted network.
Rebrand card text/colors as needed.
Test in a sandbox space: training in DM → set link → verify space card → click Mark as Completed → check status.
